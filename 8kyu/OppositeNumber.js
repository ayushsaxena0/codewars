/*Very simple, given a number (integer / decimal / both depending on the language), find its opposite (additive inverse).

Examples:

1: -1
14: -14
-34: 34 */

function opposite(number) {
  return -number;
}

// PREP
// parameters -> a number, can be +ve or -ve, always a number
// return -> return its opposite, only simple return
// example -> opposite(21) -> -21
// pseudo code -> take the input num, return the -number
