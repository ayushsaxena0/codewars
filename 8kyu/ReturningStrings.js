// Make a function that will return a greeting statement that uses an input; your program should return, "Hello, <name> how are you doing today?".

// PREP
// parameter -> should be string, can be anything
// return -> simple return only
// example -> greet("Ryan")-> "Hello, Ryan how are you doing today?"
// pseudo code -> get the input, insert it in string using template literals, return the string

function greet(name) {
  return `Hello, ${name} how are you doing today?`;
}
